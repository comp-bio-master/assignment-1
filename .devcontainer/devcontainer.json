{
  "name": "Bash-only",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },
  "postCreateCommand": "sudo apt-get update -y && sudo apt-get install -y wget unzip tree && echo '✅ Ready: wget unzip tree installed'",
  "remoteUser": "vscode"
}
